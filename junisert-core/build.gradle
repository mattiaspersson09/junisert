plugins {
    id("junisert.java-library")
    id("maven-publish")
}

dependencies {
    api(project(":junisert-api"))
    api(project(":junisert-values"))

    testImplementation(project(":junisert-testunits"))
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }

    repositories {
        mavenLocal()
    }
}
